{
  "name": "livingthread",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Different apps. One living thread.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server/main.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  }
}
